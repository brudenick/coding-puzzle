package com.coding.puzzle.view.inputreader;

/**
 * Manages the interaction with the user, prompting for inputs.
 */
public interface UserInputReader {

    /**
     * Prompts the user to insert a value, and returns it.
     * @return
     */
    String getInputString();
}
